# ─── Secrets — NEVER commit ──────────────────────────────────────────────
.env
.env.*
!.env.example
*.secret
secrets/
*.key
*.pem
*.pfx
credentials*.json
connectionstrings*.json

# ─── Pharmacy business data (reports, exports, dumps) ─────────────────────
# These contain real sales / patient / financial data. Keep them OUT of git.
# The report generator writes here by default:
Αναφορές/
Εβδομαδιαία_*
Έρευνα_*.md
Ανάλυση_*.md
*_report.md
*_report.xlsx
/data/
/reports/
/private/
/local/
*.bak
*.csv
*.xlsx
# Generated report artifacts (the .typ is data-bearing too, not just the PDF)
*.pdf
*.typ
!docs/**/*.pdf

# ─── Database artifacts ──────────────────────────────────────────────────
*.mdf
*.ldf
*.bacpac
*.dmp

# ─── OS / editor noise ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
.idea/

# Local, per-pharmacy notes that must never be published
SITE-NOTES.md
**/SITE-NOTES.md
